import LoginForm from "@/features/auth/Login";

// Aquí NO hace falta poner "use client" porque esta página solo está renderizando un componente
export default function LoginPage() {
  return <LoginForm />;
}